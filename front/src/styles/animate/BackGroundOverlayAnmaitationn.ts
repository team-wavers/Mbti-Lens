import { keyframes } from "styled-components";

const BackGroundOverLayAnimation = keyframes`
    100%{
        background-color: rgba(0, 0, 0, 0.1);

    }
`;

export default BackGroundOverLayAnimation;
